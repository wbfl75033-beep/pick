# 강사 Pick — 작업 체크리스트

오픈 목표: **2026-09-15** / 작성일: 2026-08-20

---

## 0단계 · 환경 (완료)

- [x] 프로젝트 폴더 구성, git 초기화
- [x] index.html 복사 + 원본 별도 보관
- [x] `db/schema.sql` — 제공 스키마 + 줌 필드 4개 보정
- [x] `db/seed.sql` — 기존 데이터 자동 추출 (강사 21 / 영상 63 / 공고 2 / 배너 2 / 광고 3)
- [x] `js/db.js` — 조회·저장 함수 + DB↔화면 필드명 변환
- [x] `js/auth.js` — 로그인 모달, 권한별 숨김, switchTab 가드
- [x] `js/upload.js` — Storage 업로드
- [x] `js/mail.js` — Web3Forms 발송
- [x] index.html 에 스크립트 연결, 관리자 버튼 6곳에 `data-admin-only`
- [x] vercel.json, package.json, .gitignore

---

## 1단계 · 계정 세팅 (형이 계정 받아오면 즉시)

- [x] Supabase 프로젝트 생성 — **서울 리전(ap-northeast-2)**
- [x] Storage 버킷 `pick-media` Public 생성
- [x] `db/schema.sql` 실행
- [x] `db/seed.sql` 실행
- [ ] 관리자 계정 2~3개 생성 + 외부 가입 차단
- [ ] Web3Forms Access Key 발급 (수신 minzi34@naver.com)
- [x] `js/config.js` 에 Supabase 키 입력 (Web3Forms 키는 대기 중)
- [x] 로컬에서 데이터가 화면에 뜨는지 확인

---

## 2단계 · 조회 연결 (하드코딩 배열 → DB)

`init()` 안에서 `await` 로 데이터를 받아 각 배열을 채우도록 교체.

- [x] `categories` (773행) → `PickDB.fetchCategories()`
- [x] `instructors` (775행) → `PickDB.fetchInstructors()` ※ 이용자 화면은 **instructors_public 뷰**
- [x] `posters` (1116행) → `PickDB.fetchPosters()`
- [x] `banners` (1524행) → `PickDB.fetchBanners()`
- [x] `topAds` (1552행) → `PickDB.fetchTopAds()`
- [x] `const` → `let` 변경 (배열을 새로 대입해야 함)
- [ ] 로딩 중 화면 처리 (데이터 오기 전 빈 화면 방지)
- [x] 관리자 로그인 시 본체 테이블로 다시 조회 (`fetchInstructors(true)` — 관리자 전용 번호 필요)

## 3단계 · 저장·삭제 연결

- [ ] `saveInstructor()` (2767행) → `PickDB.saveInstructor(obj, editingId)`
- [ ] `deleteInstructor()` (1188행) → `PickDB.deleteInstructor(id)`
- [ ] `savePoster()` (2938행) → `PickDB.savePoster(obj, editingPosterId)`
- [ ] `deletePoster()` (1200행) → `PickDB.deletePoster(id)`
- [ ] `saveTopAds()` (1741행) → `PickDB.saveTopAds(list)`
- [ ] `saveBanners()` (1846행) → `PickDB.saveBanners(list)`
- [ ] 저장 후 목록 새로고침 처리
- [ ] 저장 실패 시 사용자에게 알림 (현재는 무조건 "등록되었습니다" 표시)

## 4단계 · 파일 업로드 11곳 (createObjectURL → Storage)

- [ ] 강사 사진 (2800행) → `PickUpload.uploadFromInput(input, 'instructors')`
- [ ] 프로필 파일 (2802행) → `'profiles'`
- [ ] 영상 썸네일 3개 (2788행) → `'instructors'`
- [ ] 공고 포스터 (2960행) → `'posters'`
- [ ] 약도 이미지 (2962행) → `'maps'`
- [ ] 상단광고 PC·모바일 (1726·1731행) → `'ads'`
- [ ] 배너 PC·모바일 이미지 (1811·1826행) → `'banners'`
- [ ] 배너 PC·모바일 영상 (1816·1836행) → `'banners'`
- [ ] 업로드 중 로딩 표시 + 실패 처리
- [ ] 파일 교체 시 기존 Storage 파일 정리 (`PickUpload.removeByUrl`)

## 5단계 · 폼 메일 발송

- [ ] `submitInquiry()` (1464행) → `PickMail.sendInquiry({...})`
- [ ] `submitRegRequest()` (1283행) → `PickMail.sendRegRequest({...})`
- [ ] 등록요청 폼의 입력 항목 전체를 수집해서 넘기기 (현재는 필수값만 검사하고 버림)
- [ ] 실제 수신 메일함에서 제목 형식·본문 항목 확인
- [ ] **DB에 아무것도 저장되지 않는지 확인** (설계서 필수 조건)

## 6단계 · 관리자 권한 마무리

- [ ] 관리자 폼에 없는 입력칸 추가 여부 결정 → `phone_private`, `email`, `is_visible`, `sort_order`
- [ ] `specialTag`('추천강사'/'제휴강사'/'none') → DB `type`('추천'/'제휴') 매핑 확정
- [ ] 비로그인 상태에서 저장 요청이 실제로 거부되는지 테스트 (콘솔에서 직접 호출)
- [ ] 관리자 전용 번호가 이용자 화면 네트워크 응답에 안 나오는지 확인

## 7단계 · 배포

- [x] Vercel 프로젝트 생성 및 배포
- [ ] `pick.fppartners.co.kr` 도메인 연결 (CNAME 값 회사 전달)
- [ ] PC·모바일 전체 점검 (배너 슬라이드, 필터, 찜, 모달, 폼)
- [ ] Tailwind CDN 경고 확인 — 필요 시 빌드본으로 교체
- [ ] 발주처 최종 확인
