# FP파트너즈 강사 Pick

발주처(에프피파트너즈)가 **완성된 정적 HTML 프론트엔드**를 주고, 여기에 백엔드를 붙여
배포하는 프로젝트. 화면 디자인과 로직은 그대로 두고 **데이터 입출력 지점만 교체**한다.

- 프로덕션: https://pick-azure.vercel.app (최종 목표 `pick.fppartners.co.kr`)
- 오픈 목표: 2026년 9월 중순
- 상세 인수인계: `docs/인수인계서.md` ← **작업 시작 전 반드시 읽을 것**

## 기술 구성

빌드 도구 없는 순수 정적 사이트. 프레임워크 없음, npm 의존성 없음.

| | |
|---|---|
| 프론트 | `index.html` 단일 파일 (3,300여 행, Tailwind CDN, 전역 함수 + `onclick` 방식) |
| DB·인증·파일 | Supabase (서울 리전) |
| 폼 메일 | Web3Forms (DB 저장 없이 메일 발송만) |
| 배포 | Vercel (`fppartners/pick`) |

```
index.html          발주처 원본 + 수정. 원본은 docs/original/ 에 보존
js/config.js        키 3개 (전부 공개용 키. service_role 키는 절대 넣지 말 것)
js/db.js            Supabase 조회·저장 + DB컬럼↔화면필드 이름 변환
js/auth.js          관리자 로그인, 권한별 버튼 숨김, switchTab 가드
js/upload.js        Storage 업로드
js/mail.js          Web3Forms 발송
db/schema.sql       테이블·RLS·버킷 (1회 실행 완료)
db/seed.sql         초기 데이터 이관 (1회 실행 완료)
tools/              index.html 하드코딩 데이터 → seed.sql 생성 스크립트
```

## 작업 규칙

**0. 사용자는 터미널을 쓰지 않는다.**
명령어를 알려주지 말고 **직접 실행하고 결과를 설명**한다. 로컬 확인이 필요하면
개발 서버를 띄우고 브라우저로 직접 확인한 뒤 결과를 보고한다.
사용자가 직접 해야 하는 일은 **Supabase·Vercel 웹 화면에서 클릭하는 것**뿐이며,
그때는 어느 메뉴에서 무엇을 누르는지 화면 기준으로 안내한다.

**1. 발주처 화면을 임의로 바꾸지 않는다.**
디자인·레이아웃·문구는 발주처 자산이다. 스펙과 코드가 안 맞으면 **고치지 말고
`docs/확인필요사항.md` 에 기록하고 발주처에 확인**을 받는다. 지시 없는 개선은 하지 않는다.

**2. 이용자 화면은 반드시 `instructors_public` 뷰로 조회한다.**
`instructors` 본체에는 관리자 전용 연락처(`phone_private`)가 있다. 공개 뷰는 그 컬럼이
물리적으로 빠져 있어 코드가 잘못돼도 유출되지 않는다. 관리자 화면일 때만 본체를 조회한다.

**3. 문의·등록요청 폼 내용은 DB에 저장하지 않는다.**
설계서의 명시 조건. 메일 발송만 하고 어떤 테이블에도 기록을 남기지 않는다.

**4. 비밀키를 코드에 넣지 않는다.**
`anon`(publishable) 키와 Web3Forms 키는 브라우저 공개용이라 커밋해도 된다.
Supabase `service_role`/`sb_secret_` 키는 절대 사용하지 않는다. 권한은 RLS가 막는다.

**5. 배포에서 내부 자료를 제외한다.**
`.vercelignore` 로 `docs/ tools/ db/` 를 막아뒀다. 발주처 요청서·설계서·DB 구조가
공개 URL로 노출되면 안 된다. 새 폴더를 만들면 이 목록을 확인할 것.

## 명령어

```bash
npm run dev                  # 로컬 서버 http://localhost:5173
npm run seed                 # index.html 데이터 → db/seed.sql 재생성
vercel deploy --prod --yes   # 프로덕션 배포
```

관리자 진입: 푸터 맨 아래 `관리자 로그인` 링크 / `#admin` / `Ctrl+Shift+A`

## 작업 범위 — 이것만 한다

**관리자 설정에서 하는 작업이 실제로 저장되고 화면에 즉시 반영되게 만들기,
그리고 사진·파일을 넣으면 Supabase Storage에 자동 저장되게 만들기.**

- 작업 A — 저장·삭제 함수 6개를 `PickDB.*` 에 연결 (`docs/인수인계서.md` 4장)
- 작업 B — 파일 업로드 11군데를 `PickUpload.*` 에 연결 (5장)

그 외는 **전부 완료 상태다.** Supabase 세팅, 데이터 이관, 조회 연결, 관리자 로그인·권한,
폼 메일 발송, Vercel 배포가 끝나 있다. **완료된 부분은 건드리지 않는다.**
도메인 DNS 연결은 발주처 담당자 몫이며 이 작업 범위가 아니다.

범위를 벗어나는 개선 아이디어가 보이면 **실행하지 말고 사용자에게 보고**한다.

## 검증 방법

작업 후 반드시 확인한다. 상세 절차는 `docs/인수인계서.md` 7장(완료 기준).

- 로컬에서 콘솔 에러 0건
- 비로그인 상태로 쓰기 요청 시 `42501` 거부되는지 (RLS 동작)
- 공개 뷰 응답에 `phone_private` 가 없는지
- 저장 후 DB에 실제로 값이 들어갔는지 (REST 조회로 확인)
